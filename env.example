# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=

# Contact Form (if using email service)
NEXT_PUBLIC_CONTACT_EMAIL=

# Analytics (optional)
NEXT_PUBLIC_GA_ID=

# Database (for future features)
DATABASE_URL=

# Authentication (for future features)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
EMAIL_FROM=

# Payment Processing (for future features)
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email Service
RESEND_API_KEY=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Dev-only seed protection (optional)
SEED_SECRET=

# OpenAI
# Required: API key from platform.openai.com or Azure OpenAI compatible endpoint
OPENAI_API_KEY=
# Optional: override default model used by the app
OPENAI_MODEL=gpt-4o-mini
# Optional: organization or project scoping (if used)
OPENAI_ORG=
# Optional: custom base URL for self-hosted/proxy/Azure-compatible endpoints
OPENAI_BASE_URL=
